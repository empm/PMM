``` kotlin
@Composable  
fun Pantalla1ma(navegacion: NavController) {  
    Row(        modifier = Modifier            .fillMaxWidth()            .padding(16.dp),        horizontalArrangement = Arrangement.SpaceBetween,        verticalAlignment = Alignment.CenterVertically    ) {        Text("Pantalla 1")        Button(onClick = { navegacion.navigate("P2") }) {            Text("Ir a la pantalla 2")        }    }}  
  
@Composable  
fun Pantalla2ma(navagacion: NavController) {  
    Row(        modifier = Modifier            .fillMaxWidth()            .padding(16.dp),        horizontalArrangement = Arrangement.SpaceBetween,        verticalAlignment = Alignment.CenterVertically    ) {        Text("Pantalla 2")        Button(onClick = { navagacion.popBackStack() }) {            Text("Volver atras")        }        Button(onClick = { navagacion.navigate("P3") }) {            Text("Ir a la pantalla 3")        }    }}  
  
@Composable  
fun Pantalla3ma(navegacion: NavController) {  
    Row(        modifier = Modifier            .fillMaxWidth()            .padding(16.dp),        horizontalArrangement = Arrangement.SpaceBetween,        verticalAlignment = Alignment.CenterVertically    ) {        Text("Pantalla 3")        Button(onClick = { navegacion.popBackStack() }) {            Text("Volver atras")        }        Button(onClick = { navegacion.navigate("Pfinal") }) {            Text("Ir a la pantalla final")        }    }}  
  
@Composable  
fun PantallaFinalma() {  
  
    Box(        Modifier        .fillMaxSize()        .padding(24.dp)    ) {  
        Text(            "Has llegado a la pantalla final"        )    }  
}  
  
  
@Composable  
fun MiNavegacion() {  
    val controladorDeRutas = rememberNavController()    NavHost(navController = controladorDeRutas, startDestination = "P1") {        composable("P1") { Pantalla1aa(controladorDeRutas) }        composable("P2") { Pantalla2ma(controladorDeRutas) }        composable("P3") { Pantalla3aa(controladorDeRutas) }        composable("Pfinal") { PantallaFinalma() }    }}  
  
@Preview(showBackground = true, widthDp = 550)  
@Composable  
fun GreetingPreview() {  
    NavegacionTheme {        MiNavegacion()    }}
```